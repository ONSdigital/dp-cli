---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: latest

inputs:
  - name: {{.Name}}

outputs:
  - name: build

run:
  path: {{.Name}}/ci/scripts/build.sh
